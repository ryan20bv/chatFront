import React, { Fragment } from "react";
import ChatMain from "../../components/chat/ChatMain";

const index = (props) => {
	return (
		<Fragment>
			<ChatMain />
		</Fragment>
	);
};

export default index;
