import React from "react";
import classes from "./styleChat/chatHeader.module.scss";

const ChatHeader = () => {
	return <header className={classes.chat_header}>ChatHeader</header>;
};

export default ChatHeader;
